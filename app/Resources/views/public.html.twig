{% extends "::base_public.html.twig" %}
{% block body %}
<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </a>
        <a class="brand ajaxy" href="{{ path('homepage') }}">
            <strong>TASKUL.NET</strong>
        </a>

        <div class="nav-collapse collapse">
			{% if is_granted('ROLE_USER') %}
			{{ knp_menu_render('public_logged', {'currentClass': 'active'})  }}
			{% else %}
			{{ knp_menu_render('public', {'currentClass': 'active'})  }}
			{% endif %}
            </ul>
        </div>
        <div class="hidden-tablet hidden-phone" style="color:#FF9999;margin-left:400px"><p><img width="20" src="/img/skull_logo1.png" alt="Logo" class="info-beta" style="float:left"><a class="ajaxy" href="{{ path('homepage') }}">{{'taskul.beta'|trans({},'MainBundle')|raw}}</p></div>
    </div>
  </div>
</div>
<div id="content">
{% block content%}
{% endblock content %}
</div> <!-- /content -->
<div class="row-fluid"></div>
{% endblock body %}