{% extends app.request.isXmlHttpRequest ? "::base_ajax.html.twig" : "TaskBundle::layout.html.twig" %}
    {% block body %}
    {{ parent() }}

    {% endblock %}
{% block content %}
{{ apy_breadcrumb_trail_render() }}
<div class="row-fluid sortable">
    <div class="box span12">
        <div class="box-header" data-original-title>
            <h2><i class="icon-user"></i><span class="break"></span>Task list</h2>
            <div class="box-icon">
            <a href="#" class="btn-minimize"><i class="icon-chevron-up"></i></a>
            </div>
        </div>
        <div class="box-content">
            <div class="pull-right">Search: <input id="filter" type="text" /> e.g. jun, elec, 1981 <a href="#clear" class="clear-filter" title="clear filter">X</a></div>
            <table class="table table-striped table-bordered bootstrap-datatable footable" id="list" data-filter="#filter">
                <thead>
                    <tr>
                        <th data-class="expand" data-sort-initial="true">Name</th>
                        <th data-hide="phone,tablet">Status</th>
                        <th data-hide="phone,tablet" data-sort-ignore="true">Description</th>
                        <th data-sort-ignore="true" data-hide="default,phone,tablet">Tags</th>
                        <th data-sort-ignore="true">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for entity in entities %}
                    <tr>
                        <td>{{ entity.name }}</td>
                        <td>{{ entity.status|status(entity.status)|raw }}</td>
                        <td>
                            {% set desc = entity.description|striptags %}
                            {% if desc|length > 30 %}
                            {{ desc|slice(0,30) }} ... <a class="boostrap-tp"  title="{{ desc }}" href="#"><i class="icon-download"></i></a>
                            {% else %}
                            {{ desc }}
                            {% endif %}</td>

                            <td>{{ entity.tags|labelsinactive|raw }} </td>
                            <td>
                                    {% if is_granted('VIEW', entity) %}
                                        <a href="{{ path('api_get_task', { 'id': entity.id }) }}" class="btn btn-info ajaxy" title="show"><i class="icon-zoom-in icon-white"></i></a>
                                        <a href="{{ path('api_get_task_files', { 'id': entity.id }) }}" class="btn btn-info ajaxy" title="edit files"><i class="icon-zoom-in icon-white"></i></a>
                                    {% endif %}
                                    {% if is_granted('EDIT', entity) %}
                                        <a href="{{ path('api_edit_task', { 'id': entity.id }) }}" class="btn btn-success ajaxy" title="edit"><i class="icon-edit icon-white"></i></a>
                                    {% endif %}
                                    {% if is_granted('DELETE', entity) %}
                                    <button class="btn btn-danger delete-modal-btn" type="button" data-id="{{ entity.id }}" data-target="#deleteModal" data-toggle="modal"><i class="icon-trash icon-white"></i></button>
                                    {% endif %}
                                                                </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                    <tfoot class="footable-pagination">
                    <tr>
                      <td colspan="5"><ul id="pagination" class="footable-nav" /></td>
                    </tr>
                  </tfoot>
                </table>
                <div class="form-actions" style="margin: 20px -10px -8px">
                   <a href="{{ path('api_new_task') }}" class="btn ajaxy">
                    Create a new entry
                    </a>
                </div>
        </div>

    </div><!--/box-->

</div><!--/row-->
{% endblock %}
{% block javascripts %}
{{ parent() }}
{% javascripts
    '@TaskBundle/Resources/public/js/index.js'
    %}
    <script src="{{ asset_url }}"></script>
{% endjavascripts %}
{% endblock %}