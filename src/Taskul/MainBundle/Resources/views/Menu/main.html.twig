{% extends "knp_menu.html.twig" %}

{% block label %}{% if options.allow_safe_labels and item.getExtra('safe_label') is sameas(false) %}{{ item.label|raw }}{% else %}{{ item.label }}{% endif %}{% endblock %}
